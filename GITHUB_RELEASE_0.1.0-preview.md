# ZipZip GitHub Release Draft

## Release Title
`ZipZip 0.1.0-preview`

## Tag
`v0.1.0-preview`

## Release Notes
첫 공개 테스트용 `ZipZip` 프리뷰 빌드입니다.

`ZipZip`은 한국 사용자에게 익숙한 압축 프로그램 흐름에 맞춰 만든 Windows용 아카이브 매니저입니다. 이번 프리뷰에서는 기본적인 압축 열기, 압축 풀기, 새 압축 만들기, 탐색기 우클릭 메뉴, 파일 연결, 최근 파일, 드래그 앤 드롭 흐름을 점검하는 데 집중했습니다.

### 포함된 기능
- `ZIP`, `7Z` 생성
- `ZIP`, `7Z`, `RAR`, `TAR`, `GZ`, `BZ2`, `XZ`, `ZST`, `TGZ`, `TBZ`, `TBZ2`, `TXZ`, `CAB`, `ISO`, `WIM` 등 열기/풀기 지원
- 탐색기 우클릭 메뉴
  - `여기에 풀기`
  - `새 폴더에 풀기`
  - `ZipZip으로 열기`
  - `ZipZip으로 압축하기...`
- 최근 압축 파일 목록
- 파일 연결 설정
- 압축 풀기 완료 후 `폴더 열기`

### 테스트 시 중점 확인 사항
- 설치가 정상적으로 끝나는지
- 압축 파일 더블클릭 시 `ZipZip`으로 열리는지
- 우클릭 `여기에 풀기`, `새 폴더에 풀기`가 기대대로 동작하는지
- 파일/폴더 선택 후 압축하기가 정상 동작하는지
- 설정 화면에서 파일 연결, 탐색기 메뉴, 테마 변경이 정상 저장되는지

### 알려진 점
- 현재는 `preview` 단계입니다.
- 일부 PC에서는 Windows SmartScreen 경고가 나올 수 있습니다.
- 테스트용 코드 서명이 적용되어 있지만, 정식 공개 배포용 신뢰 인증서는 아직 아닙니다.

### 설치 파일
- 파일명: `ZipZip-Setup-0.1.0-preview.exe`
- SHA256: `98D63BEE509A5B8EA3264DD53D08E2F583ACAA40D5720B2D14C9182A67BD8119`

### 피드백 요청
문제가 생기면 아래 내용을 함께 남겨 주세요.

- Windows 버전
- 어떤 파일 형식에서 발생했는지
- 어떤 동작 중 문제가 났는지
- 가능하면 스크린샷 또는 재현 순서
