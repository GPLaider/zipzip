# 집집 수동 QA 시나리오

## 목적

현재 구현된 `WinUI 3 UI + 7-Zip 어댑터 + 설정 저장 + 최근 파일 저장` 흐름을 수동으로 확인한다.

## 사전 준비

- WinUI 3 앱이 실행 가능한 개발 환경
- 테스트용 압축 파일
  - `sample.zip`
  - `nested.7z`
  - 내부 폴더가 포함된 아카이브 1개 이상
- 쓰기 가능한 테스트 폴더

## 1. 시작 화면

확인 항목:

- 앱을 열면 왼쪽 파일 트리 없이 시작 화면이 먼저 보인다.
- `압축 파일 열기`와 `새로 압축하기`가 가장 눈에 띈다.
- 최근 파일 목록이 저장된 내용과 맞는다.

예상 결과:

- 파일 관리자처럼 보이지 않는다.
- 첫 행동이 명확하다.

## 2. 압축 파일 열기

절차:

1. `압축 파일 열기` 클릭
2. `sample.zip` 선택

예상 결과:

- 아카이브 목록 화면으로 이동한다.
- 포맷 배지와 항목 수가 보인다.
- 최근 파일에 해당 파일이 추가된다.

## 3. 내부 폴더 탐색

절차:

1. 내부 폴더가 있는 아카이브 열기
2. 폴더 항목 더블클릭
3. breadcrumb 버튼 클릭
4. 뒤로 버튼 클릭

예상 결과:

- 폴더 더블클릭 시 내부 폴더 목록으로 이동한다.
- breadcrumb를 통해 상위 폴더 또는 루트로 이동할 수 있다.
- 뒤로 버튼은 먼저 상위 폴더로 이동하고, 루트일 때만 화면 뒤로 간다.

## 4. 전체 풀기

절차:

1. 아카이브 화면에서 `풀기` 클릭
2. 대상 폴더 선택 또는 기본 경로 유지
3. `새 폴더에 풀기` 체크 여부를 각각 시험

예상 결과:

- 성공 메시지가 표시된다.
- 선택한 경로에 정상적으로 파일이 풀린다.
- `새 폴더에 풀기` 선택 시 아카이브 이름 폴더가 생성된다.

## 5. 선택 풀기

절차:

1. 파일 1개 또는 여러 개 선택
2. `선택 풀기` 클릭
3. 동일한 방식으로 풀기 수행

예상 결과:

- 선택한 항목만 풀린다.
- 상태 요약에 선택 항목 수와 크기가 반영된다.

## 6. 더블클릭 단축 흐름

절차:

1. 파일 항목 1개 선택
2. 더블클릭

예상 결과:

- 선택 항목만 풀기 흐름으로 이어진다.
- 폴더는 내부 탐색, 파일은 추출 흐름으로 분기된다.

## 7. 새로 압축하기

절차:

1. 시작 화면에서 `새로 압축하기` 클릭
2. 파일 여러 개 선택
3. 압축 형식과 압축 수준 확인
4. 압축 시작

예상 결과:

- 압축이 성공하면 성공 메시지가 보인다.
- 생성된 압축 파일이 바로 열릴 수 있다.
- 설정 화면에서 저장한 기본 압축 형식/압축 수준이 반영된다.

## 8. 설정 저장

절차:

1. 설정 화면에서 테마 변경
2. 기본 압축 형식 변경
3. 기본 압축 수준 변경
4. 앱 재실행

예상 결과:

- 저장 메시지가 보인다.
- 앱 재실행 후 테마가 유지된다.
- 새로 압축하기 대화상자에 기본 형식/수준이 반영된다.

## 9. 최근 파일

절차:

1. 서로 다른 압축 파일 2~3개를 순서대로 열기
2. 시작 화면으로 돌아가기
3. 앱 재실행

예상 결과:

- 최근 파일 목록이 최신 순으로 보인다.
- 중복 항목은 위로 갱신된다.
- 재실행 후에도 목록이 유지된다.

## 10. 실패 케이스

확인 항목:

- 잘못된 암호로 열거나 풀 때 오류 메시지가 이해 가능하게 보이는가
- 없는 파일 경로를 열려고 할 때 오류가 표시되는가
- 압축 생성 실패 시 홈 화면 상태 메시지가 남는가

## 완료 기준

- 핵심 흐름: 열기 / 내부 탐색 / 전체 풀기 / 선택 풀기 / 새로 압축 / 설정 저장 / 최근 파일
- 각 흐름에서 성공 또는 실패 메시지가 보인다.
- 화면이 파일 관리자처럼 느껴지지 않고, 압축 도구다운 1차 행동 중심으로 동작한다.
